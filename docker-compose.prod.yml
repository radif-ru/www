version: "3.7"

services:
  nginx:
    restart: unless-stopped
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - www-html:/var/www
      
volumes:
  www-html:
    